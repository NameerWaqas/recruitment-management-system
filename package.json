{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "build": "lerna run build --scope=client"
  },
  "dependencies": {
    "nodemon": "^2.0.7",
    "react-reveal": "^1.2.2",
    "react-scripts": "^4.0.3"
  }
}
